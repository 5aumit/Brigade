#!/usr/bin/env bash
set -euo pipefail

SCRIPT_PATH=$(readlink -f -- "${BASH_SOURCE[0]}")
STACK_ROOT=$(cd -- "$(dirname -- "$SCRIPT_PATH")/.." && pwd -P)
exec python3 "$STACK_ROOT/scripts/worker.py" "$@"
