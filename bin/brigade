#!/usr/bin/env bash
set -euo pipefail

SCRIPT_PATH=$(readlink -f -- "${BASH_SOURCE[0]}")
BRIGADE_ROOT=$(cd -- "$(dirname -- "$SCRIPT_PATH")/.." && pwd -P)
exec python3 "$BRIGADE_ROOT/scripts/worker.py" "$@"
