interface:
  display_name: "Reflect Brigade"
  short_description: "Draft evidence-backed feedback from this session"
  default_prompt: "Use $reflect-brigade to inspect this session for useful Brigade feedback."
policy:
  allow_implicit_invocation: false
